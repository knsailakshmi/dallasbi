version: 2

sources:
  - name: transactions  # Specify your source name here
    database: Prod

    freshness:
      warn_after: {count: 12, period: hour}
      error_after: {count: 24, period: hour}

    loaded_at_field: _etl_loaded_at

    tables:
      - name: transactions  # Specify the table name
